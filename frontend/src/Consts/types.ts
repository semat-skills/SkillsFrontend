export type LoginValues = {
  email: string;
  password: string;
};

export type SignupValues = {
  fullname: string;
  email: string;
  password: string;
  confirmPassword: string;
  acceptTerms: boolean;
};

export type CardValues = {
  img: string;
  title: string;
  text: string;
};

export type NewUser = {
  fullname: string;
  email: string;
  password: string;
};


