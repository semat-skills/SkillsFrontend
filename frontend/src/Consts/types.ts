
export type LoginValues = {
    email: string;
    password: string;
  };

export type SignupValues = {
    fullname: string;
    email: string;
    password: string;
    confirmPassword: string;
    acceptTerms: boolean;
  };

  export type CardValues = {
    cardName: string;
    cardContent: string;
    cardImg: string;
  };
  