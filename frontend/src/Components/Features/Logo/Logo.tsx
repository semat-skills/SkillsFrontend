import React from 'react'
import {LogoDiv} from './LogoStyle'

interface LogoProps {

}

export const Logo: React.FC<LogoProps> = ({}) => {
        return (
            <LogoDiv>
                Logo
            </LogoDiv>
           
        );
}